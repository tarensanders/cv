# Awards

```{r awards}
awards %>%
  mutate(Year = arrange(desc(Year))) %>%
  brief_entries(
    when = Year,
    what = glue("\\textbf{<Award>} <Org>. <Details>",
      .na = "", .open = "<", .close = ">"
    ),
    .protect = FALSE
  )
```
