# Service

```{r service}
detailed_entries(
  service,
  with = Type,
  what = Desc,
  why = Details
)
```

# Self-Development

```{r development}
brief_entries(
  development,
  when = Year,
  what = glue("\\textbf{<Course>.} <Location>",
    .na = "", .open = "<", .close = ">"
  ),
  .protect = FALSE
)
```
